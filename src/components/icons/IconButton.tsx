// This file previously imported and used IconButton from itself, which is incorrect. Make sure only the correct export is present.
import { Icon } from 'components/icons/CustomIcon';
import { IconButton as ChakraIconButton, IconButtonProps } from '@chakra-ui/react';
import { IconType } from 'react-icons';
import { ElementType } from 'react';